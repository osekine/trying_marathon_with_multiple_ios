name: trying_patrol_marathon_ios
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  patrol: ^3.15.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

patrol:
  app_name: My App
  android:
    package_name: com.example.tryingPatrolMarathonIos
  ios:
    bundle_id: com.example.tryingPatrolMarathonIos
  macos:
    bundle_id: com.example.tryingPatrolMarathonIos
